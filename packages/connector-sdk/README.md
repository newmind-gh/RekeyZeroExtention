# rekeyzero-connector-sdk

Connector contracts for applying canonical RekeyZero information to downstream systems.

Capabilities include structured submit/update/send/upload/fetch plus first-class `browser_use` and document generation. Connectors execute; they do not own canonical business state.
