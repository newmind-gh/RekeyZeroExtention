# RekeyZero Schemas

Shared JSON Schemas for provider-agnostic RekeyZero contracts.

`canonical-record.schema.json` defines the canonical record envelope and evidence references. Customer/industry business schemas are versioned configuration rather than hard-coded core transaction types.
